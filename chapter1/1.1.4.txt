//第1章顺序程序设计 1.1基本练习第4题
4、用程序语句表示语句
（a）
s=n*(n-1)/2;

(b)
y=pow(x,a+pow(b,c));//注意，求幂需要用pow函数

(c)
s=sqrt(p*(p-a)*(p-b)*(p-c));

(d)
x=(log(sqrt(a+d^2))-e^26)^(5/2);
//注意，C语言中ln借用用log表示，log10表示以10为底的对数

(e)
y=sin(x)/(a*x)+abs(cos(pi*x/2));

(f)
y=2/3*pi*h*(3*(pow(a,2)+pow(b,2))+pow(h,2));

(g)
r=1/(1/r1+1/r2+1/r3);

(h)
y=x/(1+x/(3+pow(2*x,2)/(5+pow(2*x,3)/(7+pow(4*x,2)))));
